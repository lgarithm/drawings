# -*- mode: sh -*-

go_cfg(){
    [ `which go` ] && return

    if [ `which gvm` ]; then
        gvm use go1.5
        return
    fi

    if [ -d "$HOME/go" ]; then
        export PATH=$PATH:$HOME/go/bin
        export GOROOT=$HOME/go
    fi
}

go_cfg
